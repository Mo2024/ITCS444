import { DisableDatesDirective } from './disable-dates.directive';

describe('DisableDatesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDatesDirective();
    expect(directive).toBeTruthy();
  });
});
